export enum Types {
    TEXT = "text",
    TEXTAREA = "textarea",
    EMAIL = "email",
    NUMBER = "number",
    CHECKBOX = "checkbox",
    RADIO = "radio",
    SELECT = "select",
    DATE = "date",
    DATETIME_LOCAL = "datetime-local",
    FILE = "file",
}